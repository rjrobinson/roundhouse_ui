<% if @page > 1 || @has_next %>
  <div class="rh-pager">
    <% if @page > 1 %>
      <%= link_to "← Prev", filter_url(page: @page - 1), class: "rh-btn" %>
    <% else %>
      <span class="rh-btn" style="opacity:.4; pointer-events:none">← Prev</span>
    <% end %>
    <span class="rh-sub">Page <%= @page %></span>
    <% if @has_next %>
      <%= link_to "Next →", filter_url(page: @page + 1), class: "rh-btn" %>
    <% else %>
      <span class="rh-btn" style="opacity:.4; pointer-events:none">Next →</span>
    <% end %>
  </div>
<% end %>
